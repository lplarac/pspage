const main=document.querySelector('main');
function mapView(){return `<div class="wrap"><div class="intro"><div><div class="eyebrow">An interactive guide to Benjamin Enke’s review</div><h1>How thinking shapes economic choices.</h1><p>The same cognitive processes can connect seemingly different behaviors. Explore the paper’s argument, then follow an idea all the way to its source.</p></div><div class="layer-key">Five depths of reading<br><b>Map → Ideas → Technical → Paper → Unpack</b></div></div>${layerPath("1",1)}<div class="map-surface"><div class="map-caption"><span>THE PAPER AT A GLANCE</span><span>Select a node to explore</span></div><a class="map-root" href="#section/1"><span class="eyebrow">§1 · The central question</span><span class="jump">↗</span><h2>What happens between information and choice?</h2><p>Explain decisions through how people acquire, interpret, and aggregate information, alongside the preferences their choices seem to reveal.</p></a><div class="connector"></div><div class="map-columns">${ATLAS.sections.slice(1,4).map((s,i)=>`<div class="node-column"><a class="map-node" href="#section/${s.id}"><span class="number">0${i+1} &nbsp; / &nbsp; SECTION ${s.id}</span><h3>${s.short}</h3><p>${s.summary}</p><span class="node-foot"><span>${i===0?'Attention · Memory':i===1?'Mental models · Similarity':'Uncertainty · Tradeoffs'}</span><span>↗</span></span></a><div class="topic-links">${s.tags.map(([id,label])=>`<a href="#idea/${id}">${label}</a>`).join('')}</div></div>`).join('')}</div><div class="crosslinks">${ATLAS.sections.slice(4,6).map(s=>`<a class="cross-node" href="#section/${s.id}"><span class="number">§${s.id}</span><div><h3>${s.short}</h3><p>${s.summary}</p></div><span class="jump">↗</span></a>`).join('')}</div></div><div class="under-map"><span>A framework for organizing evidence, not a single model of all behavior.</span><span><a href="#section/A">Methods appendix</a> &nbsp;·&nbsp; <a href="#section/B">Additional tables</a> &nbsp;·&nbsp; <a href="#read/0">Start with the abstract →</a></span></div></div>`}
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const byId=id=>ATLAS.topics.find(x=>x.id===id)||ATLAS.sections.find(x=>x.id===id);
const pageLabel=n=>n===0?'Title & abstract':`Page ${n}`;
function sidebar(id,reader=false){return `<aside class="sidebar"><div class="eyebrow">${reader?'Contents':'The paper’s argument'}</div>${reader?'<a href="#read/0">Title & abstract</a>':''}${ATLAS.sections.map(s=>`<a class="${id===s.id||id?.startsWith(s.id+'.')?'active':''}" href="#${reader?'read/'+s.page:'section/'+s.id}"><span>§${s.id}</span> &nbsp;${esc(s.short)}</a>${reader&&s.tags?s.tags.map(([t,l])=>`<a class="subnav" href="#read/${byId(t).page}/${headingAnchor(t)}">${esc(l)}</a>`).join(''):''}`).join('')}${reader?'<a href="#read/41">§7 &nbsp; References in the paper</a>':''}<a href="#map">← Back to the map</a></aside>`}
function headingAnchor(id){for(const p of PAPER.pages){const x=p.paragraphs.find(x=>x.heading&&x.section===id);if(x)return x.id}return ''}
function crumb(parts){return `<div class="crumb"><a href="#map">Paper map</a>${parts.map(([label,url])=>`<span>›</span>${url?`<a href="${url}">${esc(label)}</a>`:`<span>${esc(label)}</span>`}`).join('')}</div>`}
function related(ids){return ids?.length?`<div class="related"><div class="eyebrow">Connected ideas</div>${ids.map(id=>{const x=byId(id);return x?`<a href="#${id.includes('.')?'idea':'section'}/${id}">§${id} ${esc(x.title)} ↗</a>`:''}).join('')}</div>`:''}
function sourceButton(item){return `<a class="primary" href="#technical/${item.id}">Layer 3 · Technical commentary →</a><a class="secondary" href="#read/${item.page}/${headingAnchor(item.id)}">Layer 4 · Original paper →</a>`}
function evidence(ids){const list=(ids||[]).map(q=>PAPER.refs.find(r=>r.title.toLowerCase().includes(q.toLowerCase()))).filter(Boolean);return list.length?`<div class="related"><div class="eyebrow">Follow the research</div>${list.map(r=>`<div><a href="${esc(r.url||'#references/'+r.id)}" target="_blank" rel="noopener">${esc(r.label)} · ${esc(r.title)} ↗</a></div>`).join('')}</div>`:''}
function sectionView(id){const s=ATLAS.sections.find(x=>x.id===id);if(!s)return notFound();const topics=ATLAS.topics.filter(t=>t.id.startsWith(id+'.')&&(!['4.3.1','4.3.2','4.2'].includes(t.id)));return `<div class="wrap">${crumb([[`§${id} ${s.short}`]])}<div class="section-layout">${sidebar(id)}<article class="reading-column">${layerPath(id,2)}<div class="section-heading"><div class="eyebrow">Layer 02 · Section ${id}</div><h1>${esc(s.title)}</h1><p class="lead">${esc(s.summary)}</p></div><div class="explain-copy">${s.body.map(p=>`<p>${esc(p)}</p>`).join('')}</div>${topics.length?`<div class="section-topics">${topics.map(t=>`<a class="topic-card" href="#idea/${t.id}"><span class="eyebrow">§${t.id} · Page ${t.page}</span><h3>${esc(t.title)}</h3><p>${esc(t.summary)}</p><span class="card-end">Explore the idea →</span></a>`).join('')}</div>`:''}<div class="example"><div class="eyebrow">A concrete way to think about it</div><p>${esc(s.example)}</p></div><div class="note"><h3>Keep the distinction clear</h3><p>${esc(s.caution)}</p></div><div class="buttons">${sourceButton(s)}<a class="secondary" href="#map">Return to map</a></div></article></div></div>`}
function ideaView(id){const t=ATLAS.topics.find(x=>x.id===id);if(!t)return notFound();const s=byId(id.split('.')[0]);return `<div class="wrap">${crumb([[`§${s.id} ${s.short}`,`#section/${s.id}`],[`§${id} ${t.title}`]])}<div class="section-layout">${sidebar(id)}<article class="reading-column">${layerPath(id,2)}<div class="eyebrow">Layer 02 · Idea ${id}</div><h1>${esc(t.title)}</h1><p class="lead">${esc(t.summary)}</p><div class="explain-copy">${t.body.map(p=>`<p>${esc(p)}</p>`).join('')}</div><div class="example"><div class="eyebrow">Worked intuition · Companion explanation</div><p>${esc(t.example)}</p></div><div class="note"><h3>Read for this</h3><p>${esc(t.question)}</p><p>${esc(t.caution)}</p></div><div class="buttons">${sourceButton(t)}<a class="secondary" href="#section/${s.id}">Back to section ${s.id}</a></div>${related(t.related)}${evidence(t.refs)}</article></div></div>`}
function citations(ids){return ids?.length?`<div class="citation-links" aria-label="Research cited in this passage">${ids.map(id=>{const r=PAPER.refs.find(x=>x.id===id);return r?`<a href="${esc(r.url||'#references/'+id)}" ${r.url?'target="_blank" rel="noopener"':''} title="${esc(r.title)}">${esc(r.label)} ↗</a>`:''}).join('')}</div>`:''}
function noteFor(p){return p.annotation||PASSAGE_NOTES[p.id]}
function paragraphGuide(p){const context=byId(p.section)||byId(p.section?.split('.')[0])||byId('1');const text=noteFor(p);if(!text)return '';return `<div class="guide"><div class="eyebrow">Layer 05 · Passage explanation</div><h4>${esc(p.annotationTitle||'What this passage means')}</h4><p>${esc(text)}</p><div class="guide-links"><a class="source-link" href="#unpack/${p.id}">Open this explanation →</a><a class="source-link" href="#technical/${context.id}">Technical commentary · §${context.id} →</a></div></div>`}
function paraView(p){return `<article class="passage" id="${p.id}">${(p.sourceIds||[]).filter(id=>id!==p.id).map(id=>`<span id="${id}" class="source-anchor"></span>`).join('')}${p.caption?`<div class="eyebrow">${esc(p.caption)}</div>`:''}${p.image?`<a href="${esc(p.image)}" target="_blank" rel="noopener" aria-label="Enlarge original passage"><img class="source-crop" loading="lazy" src="${esc(p.image)}" alt="Original passage from section ${esc(p.section)}; use the original PDF for selectable mathematical notation"></a>`:`<${p.heading?'h2':'p'} class="passage-text ${p.heading?'passage-heading':''}">${esc(p.text)}</${p.heading?'h2':'p'}>`}${citations(p.refs)}${!p.heading&&noteFor(p)?`<details><summary>Layer 5 · Unpack this passage</summary>${paragraphGuide(p)}</details>`:''}</article>`}
function readerTools(n,mode){return `<div class="reader-tools"><div><button data-page="${n-1}" ${n===0?'disabled':''} aria-label="Previous paper page">←</button> <label class="sr-only" for="page-select">Paper page</label><select id="page-select">${PAPER.pages.map(p=>`<option value="${p.number}" ${p.number===n?'selected':''}>${pageLabel(p.number)}${p.number>=41&&p.number<=56?' · References':p.number>=57?' · Appendix':''}</option>`).join('')}</select> <button data-page="${n+1}" ${n===66?'disabled':''} aria-label="Next paper page">→</button></div><a class="source-link" href="#read/${n}/${mode==='original'?'':'original'}">${mode==='original'?'Paragraph reader':'Original page'}</a></div>`}
function readerView(n,mode){if(!Number.isInteger(n)||!PAPER.pages[n])return notFound();const page=PAPER.pages[n],isOriginal=mode==='original';const context=byId(page.paragraphs.find(x=>!x.heading)?.section)||byId(page.section)||byId(page.section?.split('.')[0])||byId('1');const bibliography=n>=41&&n<=56;return `<div class="wrap">${crumb([['Full paper'],[pageLabel(n)]])}<div class="reader">${sidebar(context.id,true)}<div class="reader-body">${layerPath(context.id,4,n)}<div class="eyebrow">Layer 04 · The original paper</div><h1>${pageLabel(n)}</h1><div class="page-meta">${bibliography?'Bibliography':esc(context.title)} · ${n+1} of 67 PDF pages</div>${readerTools(n,mode)}${isOriginal||page.rotated||page.tableOnly?`<a href="pages/${n}.webp" target="_blank" rel="noopener" aria-label="Enlarge original page ${n}"><img class="original-image" src="pages/${n}.webp" alt="Original paper page ${n}${page.rotated?', table rotated for reading':''}"></a>${citations(page.refs)}${page.rotated||page.tableOnly?`<div class="note"><div class="eyebrow">Layer 05 · Table explanation</div><h3>Read this evidence table</h3><p>${n===66?'This appendix table groups evidence by the source of complexity: aggregation, the number of elements, and field settings. Compare what changes with the empirical method and result.':n===9?'Compare the direction of the incentive effect with its magnitude. Attention can respond to incentives while responding less than a model predicts.':n===30?'Across rows, look for the decision, the fundamental it responds to, and the evidence tying reduced sensitivity to uncertainty or complexity.':'The columns separate the behavioral phenomenon, the proposed cognitive mechanism, and the type of evidence supporting it.'}</p><a class="source-link" href="#section/${context.id.split('.')[0]}">Return to the section guide →</a></div>`:''}`:bibliography?`<p>The paper’s references appear below with links to their source versions. Citation years are preserved from the supplied review.</p>${PAPER.refs.filter(r=>r.page===n).map(refCard).join('')}<details class="original-details"><summary>See the original bibliography page</summary><img class="original-image" src="pages/${n}.webp" loading="lazy" alt="Original bibliography page ${n}"></details>`:`<div class="page-meta">Verbatim source passages. Line breaks are reflowed; page-spanning passages continue on the next page. Expand a passage for a companion explanation.</div>${readerBlocks(page).map(paraView).join('')}<details class="original-details"><summary>Compare with the complete original page</summary><a href="pages/${n}.webp" target="_blank" rel="noopener"><img class="original-image" loading="lazy" src="pages/${n}.webp" alt="Original paper page ${n}"></a></details>`}<div class="buttons">${n>0?`<a class="secondary" href="#read/${n-1}">← ${pageLabel(n-1)}</a>`:''}${n<66?`<a class="primary" href="#read/${n+1}">${pageLabel(n+1)} →</a>`:''}<a class="secondary" href="Cognitive_turn.pdf#page=${n+1}" target="_blank" rel="noopener">Open in PDF ↗</a></div></div></div></div>`}
function refCard(r){return `<article class="ref" id="${r.id}"><div class="eyebrow">${esc(r.label)}</div><h3>${r.url?`<a href="${esc(r.url)}" target="_blank" rel="noopener">${esc(r.title)}</a>`:esc(r.title)}</h3><p>${esc(r.authors)} · ${esc(r.year)}<br>${esc(r.publication)}</p>${r.url?`<a href="${esc(r.url)}" target="_blank" rel="noopener">${esc(r.kind||'Open paper')} ↗</a>`:'<span>Public full text not located.</span>'}${r.note?`<p class="ref-note">${esc(r.note)}</p>`:''}<a class="ref-source" href="#read/${r.page||41}/original">Citation in the review →</a></article>`}
function refsView(target){return `<div class="wrap">${crumb([['References']])}<div class="section-layout">${sidebar('7')}<article class="reading-column"><div class="eyebrow">The research behind the review</div><h1>A connected bibliography.</h1><p>All ${PAPER.refs.length} references from the supplied paper. Follow a title to its publication or working-paper source. The review’s citation labels are preserved, including when the linked version has a newer title or date. For Contagious Beliefs and Recovering Economic Preferences under Behavioral Attenuation Bias, no public manuscript was located; those entries explicitly link to an author’s research listing.</p><div class="ref-letters">${[...new Set(PAPER.refs.map(r=>r.authors[0].toUpperCase()))].sort().map(l=>`<a href="#references/${PAPER.refs.find(r=>r.authors[0].toUpperCase()===l).id}">${l}</a>`).join('')}</div>${PAPER.refs.map(refCard).join('')}</article></div></div>`}
function notFound(){return `<div class="wrap"><h1>This part of the atlas could not be found.</h1><a class="primary" href="#map">Return to the paper map</a></div>`}
function render(){const [route='map',id,mode]=location.hash.replace(/^#/,'').split('/');let view;try{view=route==='technical'?technicalView(id):route==='unpack'?unpackView(id):route==='section'?sectionView(id):route==='idea'?ideaView(id):route==='read'?readerView(Number(id),mode):route==='references'?refsView(id):route==='map'||!route?mapView():notFound()}catch(error){console.error(error);view=notFound()}main.innerHTML=view;for(const a of document.querySelectorAll('[data-nav]'))a.classList.toggle('active',a.dataset.nav===(['section','idea','unpack'].includes(route)?'map':route));document.querySelectorAll('[data-page]').forEach(b=>b.addEventListener('click',()=>location.hash=`read/${b.dataset.page}`));document.querySelector('#page-select')?.addEventListener('change',e=>location.hash=`read/${e.target.value}`);const anchor=route==='references'?id:route==='read'&&mode!=='original'?mode:null;window.scrollTo(0,0);if(anchor&&/^[a-z0-9-]+$/.test(anchor))document.getElementById(anchor)?.scrollIntoView({block:'start'});document.title=(route==='technical'?(TECHNICAL[id]?.title||'Technical commentary'):route==='unpack'?'Passage explanation':route==='read'?pageLabel(Number(id)):['section','idea'].includes(route)?byId(id)?.title:route==='references'?'References':'A reading atlas')+' · The Cognitive Turn';main.focus({preventScroll:true})}
addEventListener('hashchange',()=>{if(location.hash==='#main'){main.focus();return}render()});render();
